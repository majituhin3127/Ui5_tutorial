<mvc:View
    controllerName="znorthwind.controller.Products"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
>
    <Page
        id="Products"
        title="{i18n>Productstitle}"
    >
        <!-- <List
            backgroundDesign="Transparent"
            headerText="{i18n>categoriesTitle}"
            items="{/Categories}"
        >
            <items>
                <StandardListItem
                    title="{CategoryName}"
                    description="{Description}"
                    type="Navigation"
                    press="onClick"
                />
            </items>
        </List> -->
    </Page>
</mvc:View>

